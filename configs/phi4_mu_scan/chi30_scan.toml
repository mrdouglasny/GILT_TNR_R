# χ=30 μ² scan for φ⁴ critical point
# Array job: each task gets mu_sq from SLURM_ARRAY_TASK_ID

chi = 30
gilt_eps = 6e-6
cg_eps = 1e-10
rotate = false
lam = 1.0
kappa = 0.3
K = 32
D = 16
max_steps = 40
verbosity = 1

# μ² range: scan from -1.5 to -1.1 in steps of 0.01
# Task ID 0 → μ² = -1.50
# Task ID 1 → μ² = -1.49
# ...
# Task ID 40 → μ² = -1.10
mu_sq_start = -1.50
mu_sq_step = 0.01
# mu_sq = mu_sq_start + task_id * mu_sq_step
# Actual mu_sq set by wrapper script based on SLURM_ARRAY_TASK_ID

# For single test run:
mu_sq = -1.30
